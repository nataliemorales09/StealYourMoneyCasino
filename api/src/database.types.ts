export type Json = string | number | boolean | null | { [key: string]: Json | undefined } | Json[];

/** Course registration schema — see sql/course_registration/001_schema.sql and data/course_registration/*.csv */
export type Database = {

};
